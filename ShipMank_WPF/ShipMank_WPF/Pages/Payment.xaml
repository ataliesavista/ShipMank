<Page x:Class="ShipMank_WPF.Pages.Payment"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:ShipMank_WPF.Pages"
      xmlns:components="clr-namespace:ShipMank_WPF.Components"
      xmlns:iconPacks="http://metro.mahapps.com/winfx/xaml/iconpacks"
      mc:Ignorable="d" 
      d:DesignHeight="850" d:DesignWidth="1280"
      Title="Payment">

    <Page.Resources>
        <Style TargetType="Border" x:Key="ContentCard">
            <Setter Property="Background" Value="White" />
            <Setter Property="CornerRadius" Value="12" />
            <Setter Property="Padding" Value="25" />
            <Setter Property="Effect">
                <Setter.Value>
                    <DropShadowEffect ShadowDepth="1" BlurRadius="10" Color="#000000" Opacity="0.1" />
                </Setter.Value>
            </Setter>
        </Style>

        <Style TargetType="TextBlock" x:Key="SectionHeader">
            <Setter Property="FontSize" Value="20" />
            <Setter Property="FontWeight" Value="SemiBold" />
            <Setter Property="Foreground" Value="#333" />
            <Setter Property="Margin" Value="0,0,0,15" />
        </Style>

        <Style TargetType="TextBox">
            <Setter Property="Padding" Value="12" />
            <Setter Property="BorderBrush" Value="#DDD" />
            <Setter Property="BorderThickness" Value="1" />
            <Setter Property="VerticalAlignment" Value="Center" />
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="TextBox">
                        <Border Background="{TemplateBinding Background}" 
                                BorderBrush="{TemplateBinding BorderBrush}" 
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="6">
                            <ScrollViewer x:Name="PART_ContentHost" />
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style TargetType="TextBlock" x:Key="InputLabel">
            <Setter Property="FontSize" Value="13" />
            <Setter Property="FontWeight" Value="SemiBold" />
            <Setter Property="Foreground" Value="#555" />
            <Setter Property="Margin" Value="0,0,0,6" />
        </Style>

        <Style TargetType="RadioButton" x:Key="PaymentOption">
            <Setter Property="GroupName" Value="PaymentMethod" />
            <Setter Property="Padding" Value="15" />
            <Setter Property="BorderBrush" Value="#DDD" />
            <Setter Property="BorderThickness" Value="1" />
            <Setter Property="VerticalContentAlignment" Value="Center" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="RadioButton">
                        <Border x:Name="PART_Border"
                                Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="8"
                                Padding="{TemplateBinding Padding}"
                                Cursor="Hand">
                            <ContentPresenter />
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsChecked" Value="True">
                                <Setter TargetName="PART_Border" Property="BorderBrush" Value="#33A1E0" />
                                <Setter TargetName="PART_Border" Property="BorderThickness" Value="2" />
                                <Setter TargetName="PART_Border" Property="Background" Value="#F0F9FF" />
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Page.Resources>

    <Page.Background>
        <LinearGradientBrush StartPoint="0,0" EndPoint="0,1">
            <GradientStop Color="#FFF9AF" Offset="0"/>
            <GradientStop Color="#EBF1F5" Offset="0.5"/>
        </LinearGradientBrush>
    </Page.Background>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <Grid Grid.Row="0" Margin="40,30,40,20">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>

            <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                <iconPacks:PackIconMaterial Kind="CreditCardOutline" Width="32" Height="32" VerticalAlignment="Center" Margin="0,0,15,0" Foreground="#333"/>
                <TextBlock Text="Complete Payment" FontSize="32" FontWeight="Bold" VerticalAlignment="Center" Foreground="#333"/>
            </StackPanel>

            <Button Grid.Column="1" Click="CancelButton_Click" Cursor="Hand" Background="Transparent" BorderThickness="1" BorderBrush="#E74C3C" Padding="15,8">
                <Button.Template>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" BorderBrush="{TemplateBinding BorderBrush}" BorderThickness="{TemplateBinding BorderThickness}" CornerRadius="8">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="#FFEBEE"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Button.Template>
                <StackPanel Orientation="Horizontal">
                    <iconPacks:PackIconMaterial Kind="Close" Width="14" Height="14" Foreground="#E74C3C" VerticalAlignment="Center" Margin="8,0,8,0"/>
                    <TextBlock Text="Cancel" FontWeight="Bold" Foreground="#E74C3C" FontSize="14" VerticalAlignment="Center" Margin="0,0,8,0"/>
                </StackPanel>
            </Button>
        </Grid>

        <ScrollViewer Grid.Row="1" Margin="40,0,40,0" VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Disabled">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                </Grid.RowDefinitions>

                <Grid Grid.Row="0">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="1.2*" />
                        <ColumnDefinition Width="*" />
                    </Grid.ColumnDefinitions>

                    <Border Grid.Column="1" Style="{StaticResource ContentCard}" Margin="20,0,0,0" VerticalAlignment="Stretch">
                        <StackPanel>
                            <TextBlock Text="Customer Details" Style="{StaticResource SectionHeader}" />
                            <StackPanel Margin="0,0,0,15">
                                <TextBlock Text="Full Name" Style="{StaticResource InputLabel}" />
                                <TextBox x:Name="TxtFullName" ToolTip="Enter full name"/>
                            </StackPanel>
                            <StackPanel Margin="0,0,0,15">
                                <TextBlock Text="Email Address" Style="{StaticResource InputLabel}" />
                                <TextBox x:Name="TxtEmail" ToolTip="Enter email" />
                            </StackPanel>
                            <StackPanel Margin="0,0,0,5">
                                <TextBlock Text="Phone Number" Style="{StaticResource InputLabel}" />
                                <TextBox x:Name="TxtPhone" ToolTip="Enter phone number" />
                            </StackPanel>
                        </StackPanel>
                    </Border>

                    <Border Grid.Column="0" Style="{StaticResource ContentCard}" VerticalAlignment="Stretch">
                        <StackPanel>
                            <TextBlock Text="Order Summary" Style="{StaticResource SectionHeader}" />

                            <StackPanel Margin="0,0,0,15">
                                <TextBlock Text="SHIP DETAILS" FontSize="12" FontWeight="Bold" Foreground="#AAA" Margin="0,0,0,5"/>
                                <TextBlock x:Name="TxtNamaKapal" Text="Going Merry" FontWeight="Bold" FontSize="20" Foreground="#33A1E0"/>
                                <TextBlock x:Name="TxtShipType" Text="Speedboat VIP Class" FontSize="14" Foreground="Gray" Margin="0,2,0,0"/>
                            </StackPanel>
                            <Separator Background="#EEE" Margin="0,0,0,15"/>

                            <Grid Margin="0,0,0,15">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="Auto"/>
                                    <ColumnDefinition Width="*"/>
                                </Grid.ColumnDefinitions>
                                <Grid.RowDefinitions>
                                    <RowDefinition Height="Auto"/>
                                    <RowDefinition Height="Auto"/>
                                    <RowDefinition Height="Auto"/>
                                </Grid.RowDefinitions>

                                <iconPacks:PackIconMaterial Grid.Row="0" Grid.Column="0" Kind="CalendarRange" Width="16" Height="16" Margin="0,0,10,10" Foreground="#33A1E0" VerticalAlignment="Top"/>
                                <StackPanel Grid.Row="0" Grid.Column="1" Margin="0,0,0,10">
                                    <TextBlock Text="Departure Date" FontSize="12" Foreground="Gray"/>
                                    <TextBlock x:Name="TxtDateBerangkat" Text="Senin, 12 Oktober 2025" FontWeight="SemiBold"/>
                                </StackPanel>

                                <iconPacks:PackIconMaterial Grid.Row="1" Grid.Column="0" Kind="MapMarker" Width="16" Height="16" Margin="0,0,10,10" Foreground="#E74C3C" VerticalAlignment="Top"/>
                                <StackPanel Grid.Row="1" Grid.Column="1" Margin="0,0,0,10">
                                    <TextBlock Text="Location / Route" FontSize="12" Foreground="Gray"/>
                                    <TextBlock x:Name="TxtLokasi" Text="Marina Ancol -> Pulau Bidadari" FontWeight="SemiBold" TextWrapping="Wrap"/>
                                </StackPanel>

                                <iconPacks:PackIconMaterial Grid.Row="2" Grid.Column="0" Kind="AccountGroup" Width="16" Height="16" Margin="0,0,10,0" Foreground="#FEEBAE" VerticalAlignment="Top"/>
                                <StackPanel Grid.Row="2" Grid.Column="1">
                                    <TextBlock Text="Capacity" FontSize="12" Foreground="Gray"/>
                                    <TextBlock x:Name="TxtKapasitas" Text="10 Passengers" FontWeight="SemiBold"/>
                                </StackPanel>
                            </Grid>

                            <Separator Background="#EEE" Margin="0,5,0,15"/>

                            <Grid Margin="0,0,0,5">
                                <TextBlock Text="Trip Price" HorizontalAlignment="Left" Foreground="#555" />
                                <TextBlock x:Name="TxtHargaPerjalanan" Text="Rp 1.500.000" HorizontalAlignment="Right" FontWeight="SemiBold"/>
                            </Grid>
                            <Grid Margin="0,0,0,15">
                                <TextBlock Text="Service Fee" HorizontalAlignment="Left" Foreground="#555" />
                                <TextBlock Text="Rp 5.000" HorizontalAlignment="Right" FontWeight="SemiBold"/>
                            </Grid>
                            <Border Background="#F0F9FF" CornerRadius="8" Padding="15">
                                <Grid>
                                    <TextBlock Text="Total Payment" HorizontalAlignment="Left" VerticalAlignment="Center" FontWeight="Bold" FontSize="16"/>
                                    <TextBlock x:Name="TxtTotal" Text="Rp 1.505.000" HorizontalAlignment="Right" VerticalAlignment="Center" FontWeight="Bold" FontSize="20" Foreground="#33A1E0"/>
                                </Grid>
                            </Border>

                            <Border Background="#FFFBEB" BorderBrush="#FEEBAE" BorderThickness="1" CornerRadius="6" Padding="15" Margin="0,15,0,0">
                                <StackPanel Orientation="Horizontal">
                                    <iconPacks:PackIconMaterial Kind="Lock" Width="16" Height="16" Margin="0,0,10,0" VerticalAlignment="Top" Foreground="#F59E0B"/>
                                    <TextBlock TextWrapping="Wrap" FontSize="12" Foreground="#6B4D03">
                                        Your payment is secure and encrypted. Your data is protected.
                                    </TextBlock>
                                </StackPanel>
                            </Border>
                        </StackPanel>
                    </Border>
                </Grid>

                <Border Grid.Row="1" Style="{StaticResource ContentCard}" Margin="0,20,0,20">
                    <StackPanel>
                        <TextBlock Text="Select Payment Method" Style="{StaticResource SectionHeader}" />

                        <Grid Margin="0,10,0,0">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*" />
                                <ColumnDefinition Width="*" />
                            </Grid.ColumnDefinitions>
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="Auto"/>
                            </Grid.RowDefinitions>

                            <RadioButton x:Name="RbBca" Grid.Row="0" Grid.Column="0" Style="{StaticResource PaymentOption}" Tag="bca" IsChecked="True" Margin="0,0,10,10">
                                <StackPanel Orientation="Horizontal">
                                    <Border Width="40" Height="40" CornerRadius="20" Background="#E0F2FE" Margin="0,0,10,0">
                                        <iconPacks:PackIconMaterial Kind="Bank" Width="20" Height="20" Foreground="#0EA5E9" VerticalAlignment="Center" HorizontalAlignment="Center"/>
                                    </Border>
                                    <StackPanel VerticalAlignment="Center">
                                        <TextBlock Text="BCA" FontWeight="SemiBold"/>
                                        <TextBlock Text="Virtual Account" FontSize="11" Foreground="Gray"/>
                                    </StackPanel>
                                </StackPanel>
                            </RadioButton>

                            <RadioButton x:Name="RbBni" Grid.Row="0" Grid.Column="1" Style="{StaticResource PaymentOption}" Tag="bni" Margin="10,0,0,10">
                                <StackPanel Orientation="Horizontal">
                                    <Border Width="40" Height="40" CornerRadius="20" Background="#E0F2FE" Margin="0,0,10,0">
                                        <iconPacks:PackIconMaterial Kind="Bank" Width="20" Height="20" Foreground="#0EA5E9" VerticalAlignment="Center" HorizontalAlignment="Center"/>
                                    </Border>
                                    <StackPanel VerticalAlignment="Center">
                                        <TextBlock Text="BNI" FontWeight="SemiBold"/>
                                        <TextBlock Text="Virtual Account" FontSize="11" Foreground="Gray"/>
                                    </StackPanel>
                                </StackPanel>
                            </RadioButton>

                            <RadioButton x:Name="RbBri" Grid.Row="1" Grid.Column="0" Style="{StaticResource PaymentOption}" Tag="bri" Margin="0,10,10,0">
                                <StackPanel Orientation="Horizontal">
                                    <Border Width="40" Height="40" CornerRadius="20" Background="#E0F2FE" Margin="0,0,10,0">
                                        <iconPacks:PackIconMaterial Kind="Bank" Width="20" Height="20" Foreground="#0EA5E9" VerticalAlignment="Center" HorizontalAlignment="Center"/>
                                    </Border>
                                    <StackPanel VerticalAlignment="Center">
                                        <TextBlock Text="BRI" FontWeight="SemiBold"/>
                                        <TextBlock Text="Virtual Account" FontSize="11" Foreground="Gray"/>
                                    </StackPanel>
                                </StackPanel>
                            </RadioButton>

                            <RadioButton x:Name="RbMandiri" Grid.Row="1" Grid.Column="1" Style="{StaticResource PaymentOption}" Tag="echannel" Margin="10,10,0,0">
                                <StackPanel Orientation="Horizontal">
                                    <Border Width="40" Height="40" CornerRadius="20" Background="#E0F2FE" Margin="0,0,10,0">
                                        <iconPacks:PackIconMaterial Kind="Bank" Width="20" Height="20" Foreground="#0EA5E9" VerticalAlignment="Center" HorizontalAlignment="Center"/>
                                    </Border>
                                    <StackPanel VerticalAlignment="Center">
                                        <TextBlock Text="MANDIRI" FontWeight="SemiBold"/>
                                        <TextBlock Text="Bill Payment" FontSize="11" Foreground="Gray"/>
                                    </StackPanel>
                                </StackPanel>
                            </RadioButton>
                        </Grid>
                    </StackPanel>
                </Border>
            </Grid>
        </ScrollViewer>

        <Border Grid.Row="2" Margin="40,10,40,30" CornerRadius="28">
            <Border.Effect>
                <DropShadowEffect ShadowDepth="2" BlurRadius="10" Color="#33A1E0" Opacity="0.4"/>
            </Border.Effect>
            <Button x:Name="PayButton" Content="Confirm &amp; Pay" Height="56" FontSize="18" FontWeight="Bold" Foreground="White" Background="#33A1E0" BorderThickness="0" Cursor="Hand" Click="PayButton_Click_1">
                <Button.Template>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" CornerRadius="28">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Button.Template>
            </Button>
        </Border>
    </Grid>
</Page>